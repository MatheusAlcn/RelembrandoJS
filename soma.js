// Função que recebe dois números e retorna a soma entre eles
var soma = function(a, b) {
    return a + b;
}

// Exporta a função para ser usada em outros arquivos com require()
module.exports = soma;
