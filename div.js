// Função que recebe dois números e retorna o resultado da divisão (a / b)
var div = function(a, b) {
    // Aqui você pode adicionar um tratamento de erro se quiser, ex: dividir por zero
    return a / b;
}

// Exporta a função para outros arquivos
module.exports = div;
